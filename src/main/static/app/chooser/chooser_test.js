'use strict';

describe('myApp.chooser module', function() {

    beforeEach(module('myApp.chooser'));

    describe('chooser controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var chooserCtrl = $controller('ChooserCtrl');
            expect(chooserCtrl).toBeDefined();
        }));

    });
});